{
  "name": "micro-lending-defi",
  "version": "1.0.0",
  "description": "Micro lending DeFi protocol",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network arbitrumSepolia",
    "deploy-multi": "npx hardhat run scripts/deployMultiAsset.js --network arbitrumSepolia",
    "verify": "npx hardhat verify --network arbitrumSepolia",
    "verify-multi": "npx hardhat verify --network arbitrumSepolia"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "@nomiclabs/hardhat-etherscan": "^3.1.7",
    "hardhat": "^2.17.0"
  },
  "dependencies": {
    "@chainlink/contracts": "^1.4.0",
    "@openzeppelin/contracts": "^4.9.0",
    "dotenv": "^16.3.1",
    "ethers": "^5.8.0"
  }
}
